# Local development overrides
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/policyinsight
    username: postgres
    password: postgres

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: INFO
    com.policyinsight: DEBUG
    org.springframework.web: DEBUG

